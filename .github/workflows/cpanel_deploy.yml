# .github/workflows/cpanel_deploy.yml
# 專門用於構建 Jekyll 並透過 FTP 部署到 cPanel 伺服器

name: Deploy to cPanel via FTP

on:
  # 當 push 到 main 分支時觸發自動部署
  push:
    branches: ["main"]
  # 也允許手動從 GitHub 介面觸發
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cpanel_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # 確保使用與您 Jekyll 專案相符的 Ruby 版本 (您之前使用的是 3.2)
      - name: Setup Ruby (Required for Jekyll Build)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true  # 安裝 Gemfile.lock 中的依賴

      - name: Build Jekyll Site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production
      
      - name: 🚀 Deploy built files to cPanel /public_html (via Password SFTP)
        # 🚨 再次更換 Action！使用另一個明確支援密碼的 SFTP Action
        uses: cross-solution/action-sftp@v2.0.1 
        with:
          # 連接資訊
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 22 # 標準 SFTP 端口
          
          # 上傳 Jekyll 的輸出目錄
          localDir: ./_site/
          # 您的網站根目錄
          remoteDir: /home/xxyzyh/public_html/
          
          # 確保所有參數都使用小駝峰命名法 (camelCase)，這是這個 Action 的要求
          
          # 刪除遠端不存在於本地的文件 (這是確保 public_html 乾淨的關鍵)
          deleteRemote: true

