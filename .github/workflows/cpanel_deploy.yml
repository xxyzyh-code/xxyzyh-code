# .github/workflows/cpanel_deploy.yml
# 專門用於構建 Jekyll 並透過 FTP 部署到 cPanel 伺服器

name: Deploy to cPanel via FTP

on:
  # 當 push 到 main 分支時觸發自動部署
  push:
    branches: ["main"]
  # 也允許手動從 GitHub 介面觸發
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cpanel_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # 確保使用與您 Jekyll 專案相符的 Ruby 版本 (您之前使用的是 3.2)
      - name: Setup Ruby (Required for Jekyll Build)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true  # 安裝 Gemfile.lock 中的依賴

      - name: Build Jekyll Site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production
      
      - name: 🚀 Deploy built files to cPanel /public_html (via Password SFTP)
        # 🚨 使用目前最穩定且支持 SFTP 密碼的 Action
        uses: lins-oss/sftp-action@v2.0.0 
        with:
          # 連接資訊 (這個 Action 的參數是明確的)
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 22 # 標準 SFTP 端口
          
          # 上傳 Jekyll 的輸出目錄
          local-path: './_site/**'
          # 您的網站根目錄
          remote-path: '/home/xxyzyh/public_html/'
          
          # 確保所有參數都使用正確的格式
          
          # 刪除遠端不存在於本地的文件 (這會保持 public_html 乾淨)
          dry-run: false
          delete-remote: true
          
          # 確保排除 .git 和其他文件
          exclude: ".git*, .DS_Store, .cpanel.yml"
