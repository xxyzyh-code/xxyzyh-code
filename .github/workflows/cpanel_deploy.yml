# .github/workflows/cpanel_deploy.yml
# 專門用於構建 Jekyll 並透過 FTP 部署到 cPanel 伺服器

name: Deploy to cPanel via FTP

on:
  # 當 push 到 main 分支時觸發自動部署
  push:
    branches: ["main"]
  # 也允許手動從 GitHub 介面觸發
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cpanel_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # 確保使用與您 Jekyll 專案相符的 Ruby 版本 (您之前使用的是 3.2)
      - name: Setup Ruby (Required for Jekyll Build)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true  # 安裝 Gemfile.lock 中的依賴

      - name: Build Jekyll Site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production
      
      - name: 🚀 Deploy built files to cPanel /public_html (via SSH Key SFTP)
        # 🚨 使用最標準的 SSH 部署 Action
        uses: easingthemes/ssh-deploy@main 
        with:
          # 🚨 關鍵：使用 SSH 私鑰進行認證
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          
          # 🚨 使用這個 Action 要求的參數名稱
          REMOTE_HOST: ${{ secrets.FTP_HOST }}
          REMOTE_USER: ${{ secrets.FTP_USERNAME }}
          REMOTE_PORT: 22 # 標準 SFTP 端口
          
          # 上傳 Jekyll 的輸出目錄
          SOURCE: ./_site/
          # 您的網站根目錄 (使用絕對路徑)
          TARGET: /home/${{ secrets.FTP_USERNAME }}/public_html/
          
          # 排除並增加日誌
          EXCLUDE: ".git*, .DS_Store, .cpanel.yml"
          ARGS: "-vvv" # 詳細日誌
